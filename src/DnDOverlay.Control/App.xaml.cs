namespace DnDOverlay.Control;

/// <summary>
/// Entry point of the DM application. The generic host, dependency injection and the
/// hub as a hosted service arrive in M1a; until then this exists so the project builds
/// and the architecture test has something to look at.
/// </summary>
public partial class App : System.Windows.Application
{
}
